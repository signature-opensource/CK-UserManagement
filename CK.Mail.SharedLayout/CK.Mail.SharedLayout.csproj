<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <!-- Res/** is auto-embedded by CK.EmbeddedResources.Abstractions (buildTransitive .props/.targets).
         No explicit EmbeddedResource entry needed. -->

    <ItemGroup>
        <PackageReference Include="CK.AppIdentity.Abstractions" Version="1.0.0--ci.1" />
        <PackageReference Include="CK.Template.Fluid" Version="1.0.0--ci.1" />
    </ItemGroup>

</Project>
